from django.urls import path, re_path
from . import views as ds_views

urlpatterns = [
    
    # path('logs/', ds_views.LogList.as_view(), name='digitalsky-log-list'),
    # path('logs/<uuid:pk>', ds_views.LogDetail.as_view(), name='digitalsky-log-detail'),
      
]
